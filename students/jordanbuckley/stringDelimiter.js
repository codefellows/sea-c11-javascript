// Write a function that takes in a string separated by a separator,
// and will return an array of strings in between the separators.

function stringDelimiter(inputString, separator) {
	return inputString.split(separator);
}